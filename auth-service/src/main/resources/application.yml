server:
  port: ${PORT:8085}

spring:
  application:
    name: auth-service

#  datasource:
#    url: jdbc:mysql://localhost:3306/auth_db
#    username: exo_user
#    password: root123
#    driver-class-name: com.mysql.cj.jdbc.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: update
#    show-sql: true
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.MySQL8Dialect
#        format_sql: true

  main:
    allow-bean-definition-overriding: true

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: https://exocommerce-backend.onrender.com/eureka/

jwt:
  secret: ${JWT_SECRET}
  expiration: 3600000